/**
 * 기본 노드 실행자 등록
 *
 * 이 파일을 import하면 모든 기본 실행자가 executorRegistry에 등록됩니다.
 */

import './inputExecutors';
import './mathExecutors';
import './imageExecutors';
import './logicExecutors';
import './stringExecutors';
import './dataExecutors';
import './convertExecutors';
import './utilityExecutors';
